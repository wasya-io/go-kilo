# 実装計画: 終了確認ロジックの修正

## ゴール
ユーザーが `Ctrl+X` を押した際、未保存の変更がある場合に一度警告を表示し、再度 `Ctrl+X` を押すと正しく終了するように修正する。現在は2回目の `Ctrl+X` が無視される、あるいは予期せぬ挙動を示す問題がある。

## 現状の課題
- 終了チャネルのクローズタイミングとイベント処理の競合
- `Process` ループ内でのイベント消費と、警告フラグのリセットロジックの干渉

## 提案する変更
### app/usecase/controller
#### [MODIFY] controller.go
- `quitHandler` 内での警告表示ロジックの見直し
- `quitWarningShown` フラグのリセット条件の厳密化（不要なイベントでリセットされないようにする）

## 検証計画
### 自動テスト
- `quit_test.go` を使用して、Warning表示後に再度 `Ctrl+X` を送信し、即座に `Quit` チャネルがクローズされることを確認する。
